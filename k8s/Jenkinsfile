node {
   echo 'Hello World'

   kubernetesDeploy(kubeconfigId: 'kubeconfig-staging2',               // REQUIRED

                 configs: 'k8s/*.yaml', // REQUIRED
                 enableConfigSubstitution: false
)
}